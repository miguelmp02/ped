Manual de usuario.

Objetivo:
El objetivo de esta práctica es realizar un sencillo chat entre varios usuarios usando sockets TCP de internet.

Ejecutar:
Para ejecutar el servidor, se hará uso del comando make servidor y para el cliente make cliente. Para configurar las direcciones y puertos del servidor, se deben editar los campos correspondientes en el fichero Makefile.

Detenciones:
El cliente se detiene poniendo /salir y el servidor se detiene con ctrl + c.

Comprobaciones:
Para comprobar que los procesos cliente servidor de la aplicación contienen en sus nombres las cadenas cli7 y serv7, se usarán los siguientes comandos:
-> ps -ef | grep serv7
-> ps -ef | grep cli7
